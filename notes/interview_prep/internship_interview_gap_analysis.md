# 实习面试导向补强清单

## 当前仓库状态

- `solutions/` 下共有 **129** 份按专题整理的题解，批量编译全部通过。
- `solutions/cpp/` 下还有 **108** 份历史代码，主要用于保留原始思路与迁移前版本。
- 模板和题型归纳已经比较完整，说明你不是“只会刷题”，而是在主动沉淀方法论。

结论很明确：

这已经不是一个“刚开始刷题”的仓库，而是一个可以继续打磨成**实习面试作品集**的仓库。真正需要补的，不是总量，而是**结构展示**和**高频缺口**。

## 你现在的优势

### 1. 基础盘已经够用

当前专题覆盖：

- `dynamic_programming`: 28
- `array`: 13
- `binary_tree`: 13
- `linked_list`: 11
- `stack`: 8
- `backtracking`: 7
- `sliding_window`: 6
- `bit_manipulation`: 6
- `greedy / hash_table / heap / interval / prefix_sum`: 各 5

这意味着你在下面这些面试高频方向已经有明显积累：

- 线性 DP / 股票 DP / 路径 DP
- 二叉树递归、遍历、构造
- 链表快慢指针、反转、归并
- 栈、单调栈、滑动窗口
- 常见设计题（LRU / LFU / MinStack / RandomizedSet）

### 2. 仓库可读性比普通刷题仓库强

你的分类题解大多不是“只交答案”，而是带有：

- 题意总结
- 思路拆解
- 多解法对比
- 复杂度分析
- `main()` 测试

这对面试帮助很大，因为真正决定表现的往往不是“做过没”，而是能不能把思路讲清楚。

### 3. 模板层已经搭起来了

`templates/` 目录说明你已经开始做“题型归纳”而不是单题记忆，这一点很加分。后面只要把**模板 -> 代表题 -> 常见变体**再串得更紧，面试复习效率会很高。

## 当前最该补的短板

### 1. 双指针和二分覆盖偏弱

按当前分类目录统计：

- `two_pointers`: 1
- `binary_search`: 2
- `graph`: 3
- `trie`: 1
- **Union Find 实战题：0**

这几个方向对实习面试尤其容易被问到，原因是：

- 双指针题很常见，且常作为热身题
- 二分不是只会 `704`，很多面试更爱问“边界二分”和“二分答案”
- 图和并查集是简历不错的同学经常被追问的差异化模块

### 2. 还有 8 道高频题只在历史目录里，没有迁移到专题目录

下面这些题已经做过，但还没进入你现在主展示区 `solutions/`：

- `33` 搜索旋转排序数组
- `34` 在排序数组中查找元素的第一个和最后一个位置
- `35` 搜索插入位置
- `51` N 皇后
- `74` 搜索二维矩阵
- `124` 二叉树中的最大路径和
- `153` 寻找旋转排序数组中的最小值
- `236` 二叉树的最近公共祖先

这 8 道里，`33 / 34 / 74 / 124 / 153 / 236` 都非常值得优先迁移，因为它们正好能补你现在的面试高频缺口。

### 3. 仓库存在“展示区”和“归档区”混杂的问题

`solutions/cpp/` 里还有大量：

- `TODO`
- 占位测试
- 历史调试代码
- 与专题目录重复的旧版本

这不影响你自己复习，但如果别人第一次进入仓库，很容易分不清：

- 哪些是当前推荐版本
- 哪些只是历史备份

所以后面要坚持一个原则：

**对外展示只看 `solutions/`，`solutions/cpp/` 只作为 archive。**

## 下一批最值得补的题

如果目标是“帮助实习面试”，我建议你优先补下面这 15 道，而不是继续随机刷。

### 第一优先级：补齐高频基础短板

- `11` 盛最多水的容器
- `26` 删除有序数组中的重复项
- `33` 搜索旋转排序数组
- `34` 查找元素的第一个和最后一个位置
- `74` 搜索二维矩阵
- `153` 寻找旋转排序数组中的最小值
- `236` 二叉树的最近公共祖先

这批题解决的是：

- 双指针不够成体系
- 二分边界题过少
- 树上经典追问题不够完整

### 第二优先级：拉开和普通刷题仓库的差距

- `124` 二叉树中的最大路径和
- `127` 单词接龙
- `133` 克隆图
- `547` 省份数量
- `684` 冗余连接
- `721` 账户合并

这批题的价值在于：

- `124` 是树 DP / 后序递归的代表题
- `127 / 133` 能补图搜索和图建模
- `547 / 684 / 721` 能把并查集真正落地

### 第三优先级：补变体和面试追问

- `40` 组合总和 II
- `90` 子集 II
- `91` 解码方法
- `213` 打家劫舍 II
- `875` 爱吃香蕉的珂珂

这批题的作用是让你在面试里更从容地回答：

- “这题如果有重复元素怎么办？”
- “如果状态稍微改一下你还会写吗？”
- “这题除了普通二分，还能不能二分答案？”

## 推荐补题顺序

如果你接下来只想再刷一小轮，我建议直接按这个顺序来：

1. `33`
2. `34`
3. `153`
4. `11`
5. `26`
6. `236`
7. `124`
8. `74`
9. `547`
10. `684`
11. `127`
12. `133`

这套顺序的特点是：

- 先把最薄弱的双指针 / 二分补齐
- 再补树的高级题
- 最后补图和并查集，形成面试差异化

## 面试前复习建议

### 1. 不要再只按题号复习，要按“口播模板”复习

每道题都尽量能用 30-60 秒讲清楚：

- 为什么想到这个算法
- 核心状态/不变量是什么
- 时间复杂度为什么成立
- 哪个边界最容易错

### 2. 每个专题至少保留 1-2 道“代表题”

例如：

- 滑动窗口：`3`, `76`
- 二分：`33`, `34`, `153`
- 树：`101`, `236`, `124`
- 链表：`19`, `25`, `146`
- DP：`198`, `300`, `322`, `1143`
- 图 / 并查集：`200`, `207`, `547`, `684`

你面试前真正高频回看的，应该是这类“代表题列表”，不是整个仓库。

### 3. 对历史目录做减法，不要继续把精力花在旧代码清理上

更有效的方式是：

- 把高频题迁移到专题目录
- 保证专题目录里的版本足够完整
- 让 README 明确“推荐阅读入口”

这样就够了，不需要把 `solutions/cpp/` 全部修成精装版。

## 一句话结论

你现在最缺的不是“题量”，而是：

- 补齐 **双指针 / 二分 / 图 / 并查集** 四个面试高频短板
- 把已经做过的 **8 道高频旧题迁移到专题目录**
- 让仓库展示入口更清晰，避免旧目录稀释质量感

如果只做一件最值的事，就先把：

`33 / 34 / 153 / 236 / 124 / 547 / 684`

这一组补齐。它们对实习面试的帮助会非常直接。
